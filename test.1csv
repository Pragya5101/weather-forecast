Id,Title,Description,Preconditions,Scenario,Detailed Steps,Expected Result
1,Initial Load - No Filters,Verify initial state with no filters.,User is logged in. LMS page is accessible.,Load the LMS Requests page with no filters.,1. Navigate to the LMS Requests page.,The page loads with default settings. The "Select a Center" empty state is displayed. No data is loaded in the table.,
2,Select a Center,Verify loading of requests after selecting a center.,User is on the LMS Requests page. No center is initially selected.,Select a valid center from the filter.,1. Click on the center dropdown. 2. Select a center.,Collection requests for the selected center are displayed in the table. The loading overlay appears while data is fetched.,
3,Collection ID present on arrival,Verify correct center is selected when arriving with Collection ID,User navigates to LMS page from a collection,User has come to the LMS page with a collection ID. No center is explicitly selected.,Arrive on the page with a collection ID.,1.Simulate navigation to the LMS page with collection ID in context,The correct center from collection is pre-selected. Collection requests are immediately fetched.,
4,Search by Customer Name,Verify filtering by customer name.,User is on the LMS Requests page. A center is selected.,Enter a customer name in the search box.,1. Enter a customer name. 2. Press Enter or wait for the filter to apply.,The table displays only collection requests matching the entered customer name.,
5,Clear Center after User Change,Verify that center is cleared when changing users post mount,User is on the LMS Requests page with a center and a collection ID. A user is selected.,Change selected User after the page has fully mounted.,1.Select a user from the user dropdown. ,Collection ID is cleared.,
6,Loading Overlay Display,Verify loading overlay appears during data fetch,User is on the LMS Requests page. A center is selected.,When the table is loading,Loading overlay appears.,1.Select a center from the center dropdown. ,Loading overlay is displayed on the screen.,
7,Empty State Display,Verify empty state is displayed when no center is selected,User is on the LMS Requests page. No center is selected.,When no center is selected on the page,Empty state is displayed.,1.Navigate to the LMS Requests page without selecting any center. ,Empty state with relevant message displayed.,
8,API Failure - Initial Load,Verify error handling when the API fails on initial load.,User is logged in. LMS page is accessible. API is down or returns an error.,Load the LMS Requests page.,1. Navigate to the LMS Requests page when the API is down.,An error message is displayed to the user. The page does not crash.,
9,API Failure - Center Selection,Verify error handling when the API fails after selecting a center.,User is on the LMS Requests page. A center is selected. API is down or returns an error.,Select a center when the API is down.,1. Select a center from the dropdown when the API is down.,An error message is displayed to the user. The table remains in its previous state or displays an empty state with an error message.,
10,Invalid Customer Name Search,Verify behavior with an invalid customer name search.,User is on the LMS Requests page. A center is selected.,Search for a non-existent customer name.,1. Enter a customer name that does not exist in the system. 2. Press Enter.,The table displays an empty state with a "No results found" message.,
11,Center with No Requests,Verify behavior when a center has no collection requests.,User is on the LMS Requests page. A center is selected.,Select a center that has no collection requests.,1. Select a center known to have no collection requests.,The table displays an empty state with a message like "No collection requests found for this center.",
12,Special Characters in Search,Verify search functionality with special characters.,User is on the LMS Requests page. A center is selected.,Enter special characters in the customer name search.,1. Enter characters like !@#$%^&*() in the search box. 2. Press Enter.,The search functionality handles special characters gracefully, either by returning no results or by escaping them properly and returning relevant results.,
13,Pagination Works,Verify the number of pages displayed on screen,User is on the LMS Requests page. The page should have more than 10 collection requests.,Table has more than one page,1.Select a center which displays more than 10 requests.,The requests table should display multiple pages for navigation. Pagination buttons/controls should exist and work.,
